/* const soma = function(v1, v2) {return v1 + v2; } */

const soma = (v1, v2) => {
    return v1 + v2;
}

/* quando é somente um parametro bão precisa usar parenteses */
const nome = n => {
    return n;
}

const add = n => n + 10;

console.log(soma(10, 5));

console.log(nome("Bruno"));

console.log(add(10))